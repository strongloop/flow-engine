context:
  request.verb: ${request.method}

assembly:
  execute:
    - invoke-api:
        target-url: "http://$(target-host)/"
        verb: $(request.verb)
        timeout: 1
        username: admin
        password: f00lpr00f
