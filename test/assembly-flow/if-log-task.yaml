assembly:
  execute:
    - if:
        condition: 1===0
        execute:
          - logTask:
              parameter: task1
    - logTask:
        parameter: task2
    - if:
        condition: 1===1
        execute:
          - logTask:
              parameter: $(target-host)
    - logTask:
        parameter: task4
