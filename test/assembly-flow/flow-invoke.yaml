context:
  request.verb: ${request.method}
  request.path: ${request.path.replace('/foo/', '')}

assembly:
  execute:
    - invoke:
        target-url: "http://$(target-host)/$(request.path)"
        verb: $(request.verb)
        timeout: 1
